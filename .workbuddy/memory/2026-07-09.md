# 2026-07-09

## 代码审查：115入库（Telegram 磁力离线下载机器人）

项目：Telegram bot → 搜索磁力站（cili.py，BeautifulSoup 解析）→ 通过 CloudDrive2 gRPC `AddOfflineFiles` 提交离线下载。结构 main/bot/config/cili/clouddrive_client/models。

### 发现的问题（按严重度）
- 🔴 功能 bug：
  1. `bot.py:133-141` 回调按钮用 `dl:{idx}` 查 `user_data["last_results"]`，二次搜索会覆盖导致旧按钮下错磁力（应带 search id）。
  2. `clouddrive_client.py:105-123` 重复任务判定只在 grpc.RpcError 分支；API Token 路径 `success=False` 抛 CloudDriveError 不被当重复处理。
  3. `bot.py:112-119` 无权限时未先 `query.answer()`，按钮卡 loading。
- 🟠 健壮性：gRPC 压缩 option 用了 int 0 且未必能解决 gzip 问题；cili.py 选择器强耦合单一站点 HTML，多备用域名对详情页可能形同虚设；站点无重试/退避；config `_ids` 无容错。
- 🟡 质量：零测试；无 ruff/mypy；小清理（unquote 替代 QueryParams  hack、proto 字段名稳定取用）。

### 状态
已执行方案 A（三个功能 bug 全修）：

1. `bot.py` `download_callback`：将 `await query.answer()` 提前到 `_allowed` 鉴权之前，无权限时按钮不再卡 loading；并把搜索结果从全局 `user_data["last_results"]` 改为按 `(chat_id, message_id)` 存入 `user_data["searches"]`，回调时用 `query.message.message_id` 取结果——解决二次搜索导致旧按钮下错磁力的错位 bug。同时清理了无用的 `last_query`。
2. `clouddrive_client.py`：新增 `_add_offline_file_or_duplicate`，在 `success=False`（抛 CloudDriveError，API Token 路径）与 `grpc.RpcError` 两种情况下都做重复判定；`_is_duplicate_offline_task_error` 改为接受 `BaseException`（RpcError 取 details，其余取 str）。自动建目录后的重试也走该 helper，重复同样当成功。

已 `py_compile` 验证两文件语法通过，无遗留 `last_results/last_query` 引用。
用户后续未要求修 B（健壮性）/ C。

## 提交与推送
- 已生成提交 `5eeb693`：`fix: correct callback result mapping, duplicate offline detection, and auth spinner`（仅 bot.py、clouddrive_client.py）。
- 推送失败：沙箱环境无 GitHub 凭据（无 gh、无 SSH 私钥、GCM 在无头环境无法交互登录，且无缓存凭据）。已与用户确认由其**本机**执行 `git push origin master` 推送。
- 注意：`.workbuddy/` 为未跟踪目录（Agent 工作记忆），不应入库；已建议用户加入 .gitignore。
